[tool.poetry]
name = "grab-convert-from-libgen"
version = "0.1.0"
description = ""
authors = ["Will Meyers <will.meyers@occamsec.com>"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.27.1"
lxml = "^4.8.0"

[tool.poetry.dev-dependencies]
black = "^22.1.0"
pytest = "^7.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
