[tool.poetry]
name = "grab-fork-from-libgen"
version = "1.3.3"
description = "A fork from grab-convert-from-libgen"
readme = "README.md"
keywords=["libgen", "ebooks", "books", "api", "scraper"]
authors = ["Lamarcke <cassiolamarcksilvafreitas@gmail.com>"]
repository = "https://github.com/Lamarcke/grab-fork-from-libgen"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.27.1"
lxml = "^4.8.0"
beautifulsoup4 = "^4.10.0"

[tool.poetry.dev-dependencies]
black = "^22.1.0"
pytest = "^7.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
